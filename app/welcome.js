import {lambRoutes, waveRoutes, stereoPairs} from './routes';

export class Welcome {
    constructor() {
        this.lambRoutes = lambRoutes;
        this.waveRoutes = waveRoutes;
        this.stereoPairs = stereoPairs;
    }
}